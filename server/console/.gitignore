# Build output and dependencies — regenerated by `npm run build`
node_modules/
dist/

# Symlinked to the client frontend's node_modules during local development;
# never commit a symlink.
node_modules
